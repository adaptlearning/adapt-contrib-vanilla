<div class="buttons-inner">
    <div class="buttons-cluster clearfix">
        {{#unless _shouldDisplayAttempts}}
        <div class="buttons-marking-icon icon display-none">
        </div>
        {{/unless}}
        <button class="buttons-action" aria-label="{{_accessibility._buttonAria.submitLabel}}">{{{_buttons.submit}}}</button>
        <button class="buttons-feedback" aria-label="{{_accessibility._buttonAria.disabledLabel}}" disabled="true">{{{_buttons.showFeedback}}}</button>
    </div>
    {{#if _shouldDisplayAttempts}}
    <div class="buttons-display">
        <div class="buttons-marking-icon icon display-none">
        </div>
        <div class="buttons-display-inner">
        </div>
    </div>
    {{/if}}
</div>